---
"@usefy/use-update-effect": patch
---

Correct the README's test count from "4 tests" to the real "6 tests" (the suite covers first-render skip, StrictMode mount skip, StrictMode update, deps-change, deps-unchanged, and cleanup). Docs-only; no runtime change.
