---
"@usefy/use-window-size": minor
"@usefy/hooks": minor
---

Add `useWindowSize` — a hook for tracking the browser window size with debounce/throttle, SSR-safe initial values, scrollbar control, an `onChange` callback, and no-op re-render skipping.
