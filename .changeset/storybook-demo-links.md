---
"@usefy/virtual-keyboard": patch
"@usefy/memory-monitor": patch
---

Docs: fix the "View Storybook Demo" link. The standalone component stories now sit at the top level of the Storybook sidebar (mirroring their top-level `packages/` directory) instead of under a "Components" group, so the demo URLs changed (`virtual-keyboard--…`, `memory-monitor--…`).
